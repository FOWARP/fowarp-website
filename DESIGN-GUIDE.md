# FOWARP 웹사이트 디자인 가이드

fowarp.com 리빌드의 타이포그래피·컬러 기준 문서. 이 문서가 기준이고, 코드가 이 문서와 어긋나면 코드를 고친다.

수치는 실제 배포본에서 계산된 값을 측정한 것이다(추정값 아님).

---

## 1. 폰트

| 용도 | 폰트 | 웨이트 | 파일 |
|---|---|---|---|
| 영문·숫자·기호 | **Karlsbader Grotesk** | 400, 600 | `/fonts/KarlsbaderGrotesk-400.otf`, `-600.otf` |
| 한글 | **Pretendard Variable** | 400, 600 | CDN |

### 폰트 스택은 하나로 통일한다

```css
font-family: 'Karlsbader Grotesk', 'Pretendard Variable', Arial, sans-serif;
```

전 페이지·전 요소가 이 한 줄을 쓴다. Karlsbader 에 한글 글리프가 없어서 한글은 자동으로 Pretendard 로 떨어진다.
**영문 Karlsbader / 한글 Pretendard 규칙이 이 스택 하나로 동시에 성립한다.** 언어별로 스택을 나누지 않는다.

### 지켜야 할 것

- **600 을 쓰려면 600 서체를 반드시 선언한다.** 400 만 선언한 채 `font-weight: 600` 을 주면 브라우저가 합성 볼드를 만든다.
  맥에서는 티가 덜 나지만 **윈도우 DirectWrite 에서 눈에 띄게 두꺼워진다**(글자 픽셀량 +14.8%, 자폭은 17.7px 좁음).
- **장평(`font-stretch`)은 전 요소 100%.** 좁히거나 늘리지 않는다.
- 새 텍스트를 추가할 때 폰트 스택을 따로 쓰지 않는다. 예전에 `Arial` 이나 `Pretendard` 를 앞세운 선언이 47건 섞여 있었고
  그 탓에 마키 영문이 Arial 로, 갤러리 부제 영문이 Pretendard 로 렌더되고 있었다.

---

## 2. 크기 스케일 — 화면 폭에 비례한다

고정 px 가 아니라 두 개의 CSS 변수에 비례한다.

```css
--vw     = min(innerWidth, 2560) / 100    /* 레이아웃·여백 */
--vwFont = min(innerWidth, 1920) / 100    /* 타이포·헤더 */
```

- **타이포는 전부 `--vwFont` 를 쓴다.** 1920px 에서 의도한 크기가 나오고, 그 이상은 상한이 걸려 더 커지지 않는다.
- `--vw` 는 여백·그리드용이다. 타이포에 쓰면 2560 에서 글자만 계속 커진다.
- **모바일(767px 이하)은 비례를 버리고 고정 px 로 간다.** 비례로 두면 글자가 읽을 수 없게 작아진다.

### 계수 → 크기 환산

`calc(var(--vwFont) * N)` 에서 1920px 기준 크기는 `N × 19.2` 다.

| 계수 | 1920 기준 |
|---|---|
| 0.729 | 14px |
| 0.833 | 16px |
| 0.938 | 18px |
| 0.990 | 19px |
| 1.354 | 26px |

---

## 3. 타이포 분류

전부 5단계로 관리한다. 새 텍스트는 이 중 하나에 넣고, 여기 없는 크기를 새로 만들지 않는다.

### T1 — 타이틀 (600)

사이트에서 유일하게 굵은 텍스트. 프로젝트 이름에만 쓴다.

| 클래스 | 위치 | 1920 | 모바일 |
|---|---|---|---|
| `.gallery-title` | 갤러리 썸네일 위 프로젝트명 | 26px | 15px |
| `.info-name` | 상세페이지 프로젝트명 | 26px | 18px |

```
폰트      Karlsbader Grotesk / Pretendard   600
행간      1.2  (1920: 31.2px)
자간      -0.02em  (1920: -0.52px / 모바일 -0.3px)
컬러      #000
장평      100%
```

**자간 -0.02em 은 사이트에서 T1 에만 준다.** 굵은 글자가 벌어져 보이는 걸 잡는 용도다.

#### 웨이트는 화면 폭에 따라 다르다

| 화면폭 | `.gallery-title` 웨이트 |
|---|---|
| ~1919px | 600 |
| 1920px~ | **500** |

같은 600 이라도 26px 로 커지면 획이 굵어져 과하게 무거워 보인다. 실측 획 두께 지수 기준
1512(20.5px/600)가 0.0602 인데 1920(26px/600)은 0.0765 로 **27% 두껍다**.
1920 이상에서 500 으로 낮추면 0.0511 이 되어 목표에 가장 근접한다(−15%).

정확히는 500~600 사이(약 536)가 맞지만 해당 웨이트 파일이 없다.
`-webkit-text-stroke` 보정도 검토했으나 0.10px 만 줘도 600 보다 두꺼워져(0.0843) 쓸 수 없다.

`.info-name`(상세페이지)은 아직 전 구간 600 이다. 같은 문제를 안고 있어 정리 대상.

### T2 — 본문 (400)

프로젝트 설명 카피. 국문·영문을 나란히 놓는 자리.

| 클래스 | 위치 | 1920 | 모바일 | 계수 |
|---|---|---|---|---|
| `.info-en` | 영문 카피 | 18px | 13px | 0.938 |
| `.info-ko` | 국문 카피 | **19px** | **14px** | 0.990 |

```
행간      1.5  (영문 27px / 국문 28.5px)
자간      normal
컬러      #000
```

**국문만 1px 크다.** 같은 지정 크기에서 Pretendard 한글이 Karlsbader 영문보다 작아 보이기 때문에 시각 크기를 맞춘 것이다.
국문·영문 크기를 통일하지 않는다.

### T3 — 부제 (400)

타이틀 아래 붙는 한 줄 설명.

| 클래스 | 위치 | 1920 | 모바일 |
|---|---|---|---|
| `.gallery-sub` | 갤러리 프로젝트 부제 | 16px | 11px |
| `.info-desc` | 상세페이지 프로젝트 부제 | 16px | 12px |

```
행간      1.2
자간      normal
컬러      #000
```

부제 문구 형식은 `{브랜드명} {작업 성격}` 으로 통일한다 (예: `리터니티 스킨힐러 디바이스 디자인`).
브랜딩이 아닌 건에 `브랜드 디자인` 을 붙이지 않는다.

### T4 — UI (400)

내비게이션·버튼·마키 등 기능성 텍스트.

| 클래스 | 위치 | 1920 | 모바일 |
|---|---|---|---|
| `.header-nav a` | 헤더 메뉴 | 14px | 11px |
| `.header-cta` | Get in Touch 버튼 | 14px | 9.6px |
| `.marquee-text` | 상단 마키 영문 | 14px | 11px |
| `.marquee-text-ko` | 상단 마키 국문 | 14px | 12px |
| `.menu-toggle` | 모바일 Menu 버튼 | — | 14px (고정) |
| `.mobile-menu a` | 모바일 드롭다운 항목 | — | 14px (고정) |

```
행간      마키 1.32 / 나머지 normal
자간      normal
대소문자  text-transform: capitalize  (내비·버튼)
```

모바일 메뉴는 **행 높이 40px 고정**, 각 행 하단에 `#D9D9D9` 스트로크(마지막 항목 포함).

### T5 — 푸터 (400)

| 클래스 | 1920 | 모바일 |
|---|---|---|
| `.footer-intro` / `.footer-info` / `.footer-copy` | 14px | 12px |

```
행간      1.32
자간      normal
컬러      #000  (다크 전환 시 #fff)
```

---

## 3-1. 줄간격 종합

| 분류 | 클래스 | 배수 | 1920 실측 | 모바일 실측 |
|---|---|---|---|---|
| T1 | `.gallery-title` | 1.2 | 31.20px | 18.0px |
| T1 | `.info-name` | 1.2 | 31.20px | 21.6px |
| T2 | `.info-en` | 1.5 | 27.01px | 19.5px |
| T2 | `.info-ko` | 1.5 | 28.51px | 21.0px |
| T3 | `.gallery-sub` | 1.2 | 19.19px | 13.2px |
| T3 | `.info-desc` | 1.2 | 19.19px | 14.4px |
| T4 | `.marquee-text` / `-ko` | 1.32 | 18.48px | 14.5 / 15.8px |
| T4 | `.header-nav a` / `.header-cta` / `.menu-toggle` | `normal` | — | — |
| T4 | `.mobile-menu a` | 1 | — | 14.0px |
| T5 | `.footer-*` | 1.32 | 18.48px | 15.84px |

배수는 셋뿐이다.

- **1.2** — 타이틀·부제. 한 줄짜리 라벨이라 좁게 붙인다
- **1.5** — 본문. 여러 줄로 흐르는 카피라 넓게 연다
- **1.32** — 마키·푸터. 그 중간
- **`normal`** — 한 줄 UI 요소. 브라우저 기본값에 맡긴다(폰트 메트릭 기준 약 1.2~1.3)
- **1** — 모바일 드롭다운. 행 높이 40px 를 `align-items:center` 로 잡으므로 줄간격이 개입하면 안 된다

새 텍스트의 줄간격은 이 넷 중에서 고른다. 새 배수를 만들지 않는다.

---

## 4. 컬러

### 텍스트

| 값 | 용도 |
|---|---|
| `#000` | 기본 텍스트 전체, 헤더 활성 메뉴 |
| `#B7B7B7` | 헤더 **비활성** 메뉴 (라이트·다크 공통) |
| `#FFF` | 다크 전환 시 텍스트, CTA 버튼 글자(라이트) |

### 면·선

| 값 | 용도 |
|---|---|
| `#D9D9D9` | 세로 그리드, 가로 그리드, 메뉴 스트로크 |
| `#686868` | **다크 전환 시** 그리드 |
| `#F5F5F5` | 갤러리 썸네일 호버 배경 |
| `#080808` | 미디어 컨테이너 배경 |
| `#000` | 다크 전환 시 페이지·헤더 배경 |

### 다크 전환 규칙

일부 상세페이지는 스크롤 시 배경이 검정으로 바뀐다
(`nosugaradded`, `returnity`, `hwanghugung`, `gonyakjelly`, `gooumcookit`, `unknot`, `jjonjingeo`).

이때 **헤더와 푸터도 함께 반전한다.** 배경만 바꾸면 검정 위 검정 글자가 된다.

| 요소 | 라이트 | 다크 |
|---|---|---|
| 헤더·페이지 배경 | `#FFF` | `#000` |
| 텍스트 | `#000` | `#FFF` |
| 비활성 메뉴 | `#B7B7B7` | `#B7B7B7` |
| 로고 | 원본 | `filter: invert(1)` |
| CTA 버튼 | 검정 배경 / 흰 글자 | 흰 배경 / 검정 글자 |
| 그리드 | `#D9D9D9` | `#686868` |

전환은 배경과 같은 **0.7s 커브**로 묶는다.

---

## 5. 알려진 문제

**태블릿 세로(768~1023px) 구간에서 글자가 뭉개진다.**
이 구간이 PC 규칙을 타는데 1920 비례라 본문이 7.2px 까지 떨어진다.

| 화면폭 | 타이틀 | 부제 | 본문 |
|---|---|---|---|
| 834 (아이패드 세로) | 11.3 | 6.9 | 7.8 |
| 768 | 10.4 | 6.4 | 7.2 |

미해결. 태블릿 전용 브레이크포인트를 신설하는 방향이 유력하다.

---

## 6. 새 텍스트를 추가할 때

1. T1~T5 중 어디에 속하는지 정한다. 새 크기를 만들지 않는다.
2. 폰트 스택은 통일된 한 줄을 쓴다. 언어별로 나누지 않는다.
3. PC 는 `--vwFont` 비례, 모바일(767 이하)은 고정 px 로 별도 지정한다.
4. 다크 전환 페이지라면 `body.is-dark` 규칙에도 색을 추가한다. 빠뜨리면 검정 위 검정이 된다.
5. 검증은 눈이 아니라 계산값으로 한다 — 2560 / 1920 / 1512 / 1280 / 390 을 실측한다.

### 폰트가 제대로 적용됐는지 확인하는 법

40px 로 렌더한 텍스트의 폭을 단일 폰트 기준값과 비교한다.

- 영문 450.89 → Karlsbader 일치
- 한글 380.28 → Pretendard 일치

**띄어쓰기가 들어간 문자열로 재면 안 된다.** 공백 글리프는 Karlsbader 것이라 2.5px 차이가 난다.
공백 없는 문자열로 측정한다.
